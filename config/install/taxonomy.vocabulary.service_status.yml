langcode: en
status: true
dependencies:
  module:
    - service_request
  enforced:
    module:
      - service_request
name: Status
vid: service_status
description: 'The current status of the service service_request. '
hierarchy: 0
weight: 0
